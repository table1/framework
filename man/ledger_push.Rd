% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloud.R
\name{ledger_push}
\alias{ledger_push}
\title{Import the Local Ledger into the Cloud}
\usage{
ledger_push()
}
\value{
Invisibly, a list with \code{pushed} and \code{up_to_date} counts.
}
\description{
Reads the project's local \code{framework.db} (the SQLite ledger of record that
\code{data_save()} always maintains) and appends any digests the cloud ledger
does not yet have. This is the default way project history reaches
framework.pub: an explicit, reviewable action rather than a network call
on every save.
}
\details{
For per-save streaming instead -- tamper-evident, since each entry lands
before the file could be altered -- set \code{FW_LEDGER=live} or
\code{options: cloud_ledger: live} in the project's \code{settings.yml}.
}
\seealso{
\code{\link[=data_verify]{data_verify()}}, \code{\link[=ledger_list]{ledger_list()}}
}
