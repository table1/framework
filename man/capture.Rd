% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scratch.R
\name{capture}
\alias{capture}
\title{Capture and Save Data to File}
\usage{
capture(x, name, to = "text", location = NULL)
}
\arguments{
\item{x}{The object to save. For CSV output, must be a data frame or tibble.}

\item{name}{Character string specifying the name of the file (without extension).}

\item{to}{Character string indicating the output format. One of: "text" (default),
"rds", or "csv".}

\item{location}{Character string specifying the directory where the file should be saved.
If NULL, uses the scratch directory from the configuration.}
}
\value{
The input object \code{x} invisibly.
}
\description{
Saves data to a file in various formats (text, RDS, or CSV) in a specified location.
If no location is provided, uses the scratch directory from the configuration.
}
\examples{
# Save a character vector as text
capture(c("hello", "world"), "greeting")

# Save a data frame as CSV
capture(mtcars, "cars", to = "csv")

# Save an R object as RDS
capture(list(a = 1, b = 2), "mylist", to = "rds")

}
