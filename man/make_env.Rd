% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env.R
\name{make_env}
\alias{make_env}
\title{Create or append to .env file}
\usage{
make_env(..., comment = NULL, check_gitignore = TRUE)
}
\arguments{
\item{...}{Named arguments for environment variables (e.g., DB_PASSWORD = "secret")}

\item{comment}{Optional comment to add before the variables}

\item{check_gitignore}{Logical; if TRUE (default), warns if .env not gitignored}
}
\value{
Invisibly returns TRUE on success
}
\description{
Creates a .env file (if it doesn't exist) and appends environment variables.
Warns if .env is not in .gitignore to prevent accidental secret exposure.
}
\examples{
\dontrun{
# Create .env with database credentials
make_env(
  DB_HOST = "localhost",
  DB_PORT = "5432",
  DB_PASSWORD = "secret",
  comment = "Database connection"
)

# Add API keys
make_env(
  OPENAI_API_KEY = "sk-...",
  comment = "API credentials"
)
}
}
