---
title: "Test Notebook"
author: "Your Name"
date: today
format: html
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(framework)
scaffold()
```

## Inspect config

```{r}
config
```

## Load Data

```{r}
data <- load_data("source.private.example")
data |> head()
```

## Save Data

```{r}
save_data(data, "final.public.example")
list.files("data/final/public")
```

## Read from framework database

Query the framework.db to demonstrate database functionality:

```{r}
# Query the framework database itself to show database functionality
framework_data <- get_query("SELECT name, type FROM sqlite_master WHERE type='table'", "framework")
framework_data |> knitr::kable()
```

## Cache expensive operations

```{r}
# cache_forget("test")
results <- get_or_cache("test", {
  Sys.sleep(5)
  1 + 1
})

results
```
