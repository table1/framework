project:
  type: default

# Root-level defaults for privacy-first project
# notebooks/ has its own _quarto.yml for output configuration

execute:
  freeze: auto
  cache: true
  warning: false
  message: false
