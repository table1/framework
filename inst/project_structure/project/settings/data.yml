data:
  # Cache and scratch are now in outputs/private/
  cache_dir: outputs/private/cache
  scratch_dir: outputs/private/scratch

  # Input data catalog (read-only in code)
  inputs:
    raw:
      # example:
      #   path: inputs/private/raw/example.csv
      #   type: csv
      #   delimiter: comma
      #   locked: true
      #   codebook:
      #     name: character
      #     gender: character
      #     birthday: date
      #     birthtime: time
      #     fav_number:
      #       type: integer
      #       min: 1
      #       max: 100
    intermediate:
    examples:

  # Output data catalog (write-only, derived artifacts)
  outputs:
    tables:
    figures:
    models:
