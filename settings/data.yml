data:
  cache_dir: outputs/private/cache
  scratch_dir: outputs/private/scratch

  inputs:
    raw:
    intermediate:
    final:
    reference:

  outputs:
    tables:
    figures:
    models:
    final:
    docs:
